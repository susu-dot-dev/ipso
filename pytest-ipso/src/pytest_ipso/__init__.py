"""pytest_ipso — pytest plugin for ipso notebook cell tests.

This module is registered as a pytest plugin via the ``pytest11`` entry point.
Implementation is in ``pytest_ipso.plugin``.
"""

from pytest_ipso.__about__ import __version__

__all__ = ["__version__"]
