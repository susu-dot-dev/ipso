name: pytest-nota-bene Publish

on:
  push:
    branches: [main]
    tags: ["pytest-nota-bene/v[0-9]+.[0-9]+.[0-9]+"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: pytest-nota-bene/.python-version

      - name: Build package
        run: uv build
        working-directory: pytest-nota-bene

      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: pytest-nota-bene/dist/

  publish:
    needs: [build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/pytest-nota-bene/v')
    environment: pypi
    permissions:
      id-token: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
