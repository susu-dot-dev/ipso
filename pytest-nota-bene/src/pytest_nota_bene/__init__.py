"""pytest_nota_bene — pytest plugin for nota-bene notebook cell tests.

This module is registered as a pytest plugin via the ``pytest11`` entry point.
Implementation is in ``pytest_nota_bene.plugin``.
"""

from pytest_nota_bene.__about__ import __version__

__all__ = ["__version__"]
