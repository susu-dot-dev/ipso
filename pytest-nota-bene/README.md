# pytest-nota-bene

pytest plugin for running [nota-bene](../nota-bene) notebook cell tests.

## Installation

```
pip install pytest-nota-bene
```

No configuration needed — pytest auto-discovers `.ipynb` files once the plugin is installed.
